# レビュー範囲: masterとの差分

以下に示すのは `master` ブランチからの git diff です。各行の先頭に `[L<番号>]` が付いており、これが新ファイルでの実際の行番号です。

- `+` で始まる行が追加、`-` で始まる行が削除された内容です
- **追加・変更された箇所のみ**をレビュー対象としてください
- 削除行（`-`）は文脈把握のみに使用し、評価しないでください
- コメントを付ける際は `[L<番号>]` の番号を `line` フィールドに使用してください
- diff のないファイルについては言及不要です
